name: Gold Price Bot

on:
  schedule:
    # Chạy tự động lúc 09:00 sáng VN hàng ngày
    - cron: "0 2 * * *"
  # Dòng này cho phép bấm nút "Run workflow" để chạy NGAY LẬP TỨC
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip' # Tăng tốc độ cài đặt cho các lần sau

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Cài các thư viện từ file requirements.txt
          pip install -r requirements.txt
          # Cài thêm thư viện bổ trợ xử lý HTML cho pandas (quan trọng)
          pip install html5lib lxml

      - name: Run Gold Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python bot_gold_price.py
